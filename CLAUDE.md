# CLAUDE.md — Moquin Press QA Checklist System

Reference notes for working on this app. See also `REGRESSION_CHECKLIST.md` (manual test checklist) and `cloudflare-backend/src/index.js` (backend source, gitignored from GitHub Pages but tracked here locally).

## Architecture

- **Frontend**: single file `index.html` — plain HTML/CSS/JS, no build step, no framework. Deployed via GitHub Pages at `https://anthonymancino222.github.io/qa-checklist/`. Push to `main` on `https://github.com/anthonymancino222/qa-checklist.git` → live in about 15–30s (GitHub Pages CDN cache lag; a fetch with `cache: 'no-store'` right after push often still shows stale content for the first ~15s — wait and retry rather than assuming the push failed). Pages source is set to **"Deploy from a branch"** (main, /root), not "GitHub Actions" — deliberately switched to this because the site has no build step, and it sidesteps an Actions/Pages-environment failure mode described below. Verifying what's actually live: `Invoke-WebRequest` the site with a cache-busting query param and compare against the local file's line count (`(Get-Content file).Count` on both sides) rather than raw string/byte length — CRLF-vs-LF line endings alone can make byte lengths differ by tens of KB on a file this size with zero real content difference.
- **GitHub Pages reliability gotcha**: deploys can get stuck indefinitely in `deployment_queued` — seen for several hours straight on this repo, caused by a stale/orphaned deployment jamming the Pages environment queue, compounded by (that same day) a real GitHub-wide "Partial System Outage." Check `https://www.githubstatus.com/api/v2/status.json` before assuming it's a repo config problem. Fixes that worked: toggling Settings → Pages source away from "GitHub Actions" and back (regenerates the environment binding — may require re-creating a `.github/workflows/*.yml` via the suggested "Static HTML" template if the source toggle clears whatever was driving deploys), then simply waiting for any active GitHub outage to clear. Retrying pushes alone did not help while the outage was ongoing.
- **Backend**: Cloudflare Worker `qa-checklist-backend` (source at `cloudflare-backend/src/index.js`, **gitignored** — not pushed to GitHub, deployed manually). Base URL: `https://qa-checklist-backend.anthony-mancino2.workers.dev`. Data store: Cloudflare D1 database `qa-checklist-db` (one `records` table, JSON blob per record — see `cloudflare-backend/schema.sql`).
- **Auth**: shared header `X-API-Key: MoquinQA2026SecureKeyAlpha7` (constants `QA_BACKEND_URL` / `QA_BACKEND_API_KEY` near the top of `index.html`) — required on writes, reads are open.
- **Sync model**: offline-first. Every save writes to `localStorage` immediately; a background push/pull (`_syncPushAll`, `_syncPullAndMerge`, `_syncNowAll`) reconciles with the Worker. Retries every 30s and on `online` event. Same-id conflicts: local wins (no overwrite) except server-side deletes always win (pruned locally on pull) to avoid resurrecting deleted records.
- **Manual sync**: header "🔄 Sync" button (`headerSyncNowClick()`) next to DATA EDIT — pushes pending, pulls fresh, re-renders the current screen, updates nav badges.

## Backend deployment (no local Node/wrangler available in this environment)

The Cloudflare dashboard's in-browser code editor (Quick Edit) is used directly via browser automation or manual copy-paste — there is no `npx`/`wrangler` CLI in this environment. Gotchas:

- **Large pastes via automated typing are unreliable** — has corrupted the editor mid-paste more than once (stray keystrokes opened command-palette/search overlays, or dropped a chunk of text). For anything beyond a one-line edit, prefer having the user paste the code manually (Ctrl+A, Ctrl+V, Deploy) over automating it — always verify with a fresh `Ctrl+Home` → `Ctrl+Shift+End` style check that nothing got corrupted before deploying, and if in doubt, **discard and reload** rather than risk deploying broken code (the live Worker keeps its last-good version until you explicitly click Deploy).
- Editing the Worker's env vars: Overview → Settings → "Variables and secrets" → Add/Edit → set Type to **Secret** for anything sensitive (private keys, API keys) and **Text** for plain config (emails, IDs) → Deploy.
- The Cloudflare dashboard also surfaces unrelated upsell/onboarding flows (an "Enable Agent Lee access" panel, a "Cloudflare Zero Trust — choose a plan" page) that can appear from stray clicks/keystrokes — back out of these (Cancel/X), never grant access or select a plan.

## Google Drive photo storage

Photos (make-ready, QA release, NCR, RCA) are uploaded to a Google Drive **Shared Drive** and referenced by a backend-proxied link instead of being embedded as base64 in synced job/NCR/RCA records (which was filling up `localStorage`).

- **Why a Shared Drive, not a regular "My Drive" folder**: a service account has zero storage quota of its own. Even when a personal folder is *shared* with the service account, uploading into it still fails with `storageQuotaExceeded` — the service account is the file's creator/owner, and owners need real quota. A Shared Drive owns its files itself, sidestepping that entirely. Both the upload and retrieval Drive API calls need `&supportsAllDrives=true` for this to work against a Shared Drive.
- **Service account**: `qa-photo-uploader@qa-photo-storage.iam.gserviceaccount.com` (Google Cloud project `qa-photo-storage`), scope `drive.file`, added as a **Content Manager** member of the Shared Drive.
- **Secrets** (set as Cloudflare Worker secrets/vars — never committed to the repo): `GOOGLE_SA_EMAIL` (plaintext), `GOOGLE_SA_PRIVATE_KEY` (secret — the PEM from the service account's JSON key; needs real/stripped whitespace, not literal `\n` — see `pemToArrayBuffer` in the backend), `GOOGLE_DRIVE_FOLDER_ID` (plaintext — currently the Shared Drive's own ID, `0AHhuh7Nddb6UUk9PVA`).
- **Backend endpoints**: `POST /photo` (auth required, body `{filename, mimeType, dataBase64}` → `{id, url}`) uploads via a JWT-signed (RS256, Web Crypto) service-account token exchanged at Google's OAuth token endpoint; `GET /photo/:id` (no auth — id itself is unguessable) proxies the file bytes back through the Worker using the same service-account token, so the Drive file stays **private** and the browser never needs its own Google auth to display it.
- **Frontend helper**: `_uploadPhotoToDrive(dataUrl)` in `index.html` — POSTs the captured photo, returns the backend proxy URL (`QA_BACKEND_URL + result.url`). **On any failure it resolves to the original data URL unchanged** — a photo must never be lost to a flaky shop-floor connection; worst case it just falls back to the old embedded-base64 behavior for that one photo. Wired into every photo-commit point: `submitForQAApproval`, `beginVeritivPacking`, `beginInspection` (Kama/DC/Gluer, both production and make-ready branches), `submitJob` (Bindery/Pressroom Cutter + non-Veritiv Packing multi-product path), NCR photo capture, RCA photo capture.
- Photos taken/synced **before** this was added remain stored as base64 — no retroactive migration was done; only new captures go through Drive.

## DATA EDIT mode (`_dataEditMode`)

Global passcode-gated toggle (`toggleDataEditMode()` — passcode required only to turn ON, never to turn off, after a "stuck in edit mode" field bug). Turning it on reveals:
- Edit pencils on editable summary fields inside the job popup (only for `production`/`qa_pending`/`qa_release` stages — see `dataEditShown` in `openContinueModal`).
- A **"🗑️ Delete Job" button** — independent of the above, shown for **any** process stage as long as `_dataEditMode` is on. Two separate confirmations (differently worded) guard every delete, so an accidental/reflexive tap can't push it through.
- Delete is wired into **every** job-detail surface, because each has its own separate popup:
  - `_cmDeleteJob()` — the shared continue-modal (make ready / production / QA release / on-hold — anything opened via `openContinueModal`).
  - `_shipDeleteJob()` — the separate Shipping Approval modal (`openShippingModal`/`shipState.jobId`).
  - Per-job kanban/list delete (`deleteIPJob`, `_jpDeleteBtnHTML`) — Job in Progress tile/list cards directly.
- `_updateHeaderDataEditBtn()` is the central sync point — flipping DATA EDIT from the header button updates every open modal's delete-button visibility in one place, so a new per-modal delete button just needs to read `_dataEditMode` there rather than wiring its own toggle listener.

## Workflow — who pushes what

The user has granted standing permission for Claude to directly execute deploys in this project — push to GitHub, deploy/edit the Cloudflare Worker, edit the Apps Script backend — without asking each time. This has been the actual working pattern across multiple sessions: Claude runs `git add`/`commit`/`push` directly via PowerShell, and edits/deploys the Worker through a connected Cloudflare dashboard browser tab. **Permission is granted broadly; what actually blocks Claude is capability, not permission** — three distinct kinds of block have been confirmed, and none of them can be talked past by the user saying "you have permission":
1. **Domain allowlist** — `github.com`, Cloudflare Console, and Google Cloud Console are outright-blocked domains for the `claude-in-chrome` browser tool. Confirmed repeatedly: even explicit "you have permission to navigate there" in chat does not lift this. `script.google.com` / `docs.google.com` are *not* blocked for navigation/clicking, but see the next point.
2. **Content-reading restriction on Google domains** — on `script.google.com`/`docs.google.com`, `screenshot` and `get_page_text` are blocked (privacy protection on the user's account content) even though `navigate`/`computer` click actions work fine. Verify state there by describing click results or via direct HTTP calls (PowerShell `Invoke-WebRequest` against a public API/doGet endpoint) — never by trying to screenshot.
3. **Risky-action safety classifier** — a separate check (independent of both of the above) can block one specific *action* it judges too risky to automate, e.g. it blocked an autonomous click on Apps Script's "Deploy" button specifically, mid-session, after permission had already been established and other clicks on the same page had gone through fine. When this happens, stop retrying that exact action and hand it back to the user with precise click-by-click instructions.

For anything hitting one of the above, the user has stepped in directly:
- Google Cloud Console setup (creating the project, service account, and Shared Drive), and any Cloudflare/Google Cloud Console navigation.
- The Apps Script "Deploy" button click (blocked by the classifier — see above), and manual copy-paste into the Apps Script code editor for large/multi-line changes (Claude edits the local `.gs` file and hands the user the diff to paste; typing large pastes into that editor directly has corrupted it before — see "Backend deployment" gotchas above).
- The Gluer Shift Handoff app's GitHub upload step specifically, since that app isn't a git repo (see cross-session memory `reference-gluer-shift-handoff-deploy` / `project-moquin-press-apps-overview`) and its deploy path runs through the blocked `github.com` domain.
- Anything requiring their own Google/Cloudflare/GitHub account login, or approving a new domain/tab connection.

**Instruction for future sessions**: keep this same division of labor by default — don't ask "can I push this?" or "can I deploy this?", just do it. Only hand a step back to the user when one of the three capability blocks above is actually hit, and say so explicitly and specifically (which of the three, not just "I can't") rather than silently retrying or giving up vaguely.

## RCA module (root cause analysis) — integrated live

The RCA tab was ported in from a separate standalone app and is now **live** (`RCA_LIVE = true` near the top of `index.html`), talking to a real Google Apps Script backend (`RCA_SCRIPT_URL`, source at `Desktop\Anthony\A3 and Kaizen projects\RCCA Form\RCA_AppScript_v3.gs`) instead of local-only simulation.

- **`TESTING_ALL_TO_ANTHONY = true`** in the Apps Script backend (temporary) — every manager-directed email (send-to-manager, cross-dept root-cause notice, department hand-off) currently routes to Anthony only, regardless of station, so real department managers don't get test traffic. **Flip to `false` and redeploy once the user says he's done testing.**
- Manager/close-out flows are real **token-based deep links**: an email contains `https://anthonymancino222.github.io/qa-checklist/?mode=manager&token=RCA-XXXX` (or `?mode=closeout&...`) — opening that URL fetches the record by token via JSONP (`_rcaJsonpGet`, since Apps Script GET responses carry no CORS headers) and renders the manager-fill or close-out section read-only-above-it. A prominent full-screen loading overlay (`#rca-token-loading`) covers this fetch — it used to be silent enough that someone mistook it for a frozen page and started re-navigating mid-load.
- `rcaSendToSheets()` posts via a plain `fetch(url, {mode:'no-cors', body: JSON.stringify(data)})`, **not** a hidden-iframe form POST — the iframe technique was tried first and reliably 403'd on the Apps Script exec→echo redirect chain under Chrome's third-party-cookie restrictions. Don't revert to the iframe approach.
- The quality-issue/root-cause/corrective-action/disposition grids are **multi-select** (toggle on/off per button, comma-joined into the hidden value) — cascading root-cause/corrective-action option lists union across every selected category rather than assuming one. Grids also collapse to a one-line summary once answered (a big pill-style "Show options/Hide" toggle), staying force-expanded while unanswered so a required field can't be missed. If you ever see a selected category not "sticking" after a re-render, check that whatever set the value went through `_rcaSetGridValue()` (updates the `_rcaGridSelectedSets` map) rather than a direct `.value =` assignment on the hidden input — the latter gets silently overwritten by the next grid re-render, which reads from the map, not the input.
- Photos ride as an array (`data.photos`, Drive-proxy URLs) in the payload; the backend's `buildPhotoHtml()` helper embeds all of them inline in the email (not just the first) — if email photos ever regress to showing only one, check that helper is still being used in all four `send*Email` functions in the `.gs` file.

## Load-tag photo scanner (OCR)

Re-enabled in QC setup, RCA, and NCR forms (was previously hidden behind `display:none` pending "needs reliable internet" / no barcode on tags — both are no longer true; printed tags now carry a barcode too). Currently reads the **printed text** via Tesseract.js OCR (`_ltLoadTesseract`/`_ltRecognize`/`_ltParse`), not the barcode symbols themselves. A design discussion (not yet implemented) concluded that real barcode *decoding* — reading all of a tag's several stacked barcodes in one photo via the native `BarcodeDetector` API (with a JS-library fallback for browsers lacking it), sorting detections by vertical position to map each to its field, cross-checked against the OCR read of the adjacent printed number — is the right approach if/when this gets built, in preference to a "scan one barcode at a time" flow.

## Upload progress overlay

`submitJob()`/`beginInspection()`/`submitForQAApproval()`/`beginVeritivPacking()` all `await` one or more `_uploadPhotoToDrive()` calls (real network round trips) before the app can move on — for a multi-product run this used to be a silent multi-second wait with zero feedback, easy to mistake for a frozen app. A shared full-screen "Uploading photo(s)…" overlay (`#qc-upload-overlay`, shown/hidden via `_qcShowUploadOverlay()`/`_qcHideUploadOverlay()`) now covers every one of those call sites, with per-photo progress text ("Uploading photo 2 of 5…") in the multi-product loops (Gluer, Bindery/Pressroom Cutter). Wrap any *new* photo-upload call site the same way — show before, `try`/`finally` hide after, so it can't get stuck open on an error.

## Standing rules (also saved in Claude's cross-session memory)

- **Test cleanup**: after any testing pass in the sandbox, delete test/loose job/NCR/RCA records from **both** local storage and the live Cloudflare backend (sandbox and production share the same backend — nothing is isolated). Verify deletes held by re-querying after ~30–40s. Stop this once the user says the app has gone live; from then on only clean up Claude's own test data.
- **Never touch the CERM MIS/ERP system** from this codebase, under any circumstance.
- The sister app **Gluer Shift Handoff** shares some UI patterns (offline-first sync design, History/Records year-month drill-down) — worth checking there for precedent before designing something from scratch here.
